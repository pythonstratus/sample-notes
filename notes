# Contract type from contractData
contract_type = col("ns0:content.ns1:award.ns1:contractData.ns1:contractActionType._value").cast("string").alias("contract_type")

# Add to selected fields 
selected_fields = selected_fields + (contract_type,)
